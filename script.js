"use strict";

const ScrollReveal = require("scrollreveal");

// const scrollreveal = require("scrollreveal");

/* Reveal section */
/* const revealElement = document.querySelectorAll("[data-revealed]");
const scrollReveal = function () {
  revealElement.forEach((element) => {
    const isElementOnScreen =
      element.getBoundingClientRect().top < window.innerHeight;

    if (isElementOnScreen) {
      element.classList.add("revealed");
    } else {
      element.classList.remove("revealed");
    }
  });
};

window.addEventListener("scroll", scrollReveal);
window.addEventListener("load", scrollReveal); */

/* const scrollRevealBottom = {
  distance: "50px",
  origin: "bottom",
  duration: 1000,
};

console.log("Hero heading revealed!");

ScrollReveal().reveal(".hero .heading-primary", scrollRevealBottom); */
